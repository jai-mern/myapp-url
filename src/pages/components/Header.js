import React from 'react';

// Header component to display the header of the page
const Header = () => {
    return (
        <header>
            <h1>Header Component</h1>
        </header>
    );
}

export default Header;
